# dependencies
/node_modules

# next.js
/.next
/out

# env — never commit real keys
.env
.env.local

# misc
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo
docs/

# NOTE: data/knowledge-base.json and public/manual/*.png ARE committed
# so the app runs without re-running extraction.
